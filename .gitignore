# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Logs
*.log
npm-debug.log*

# Local environment (sekrety)
.env
.env.local
.env.*.local

# NextJS experimental — zamrożony eksperyment, nie wchodzi do repo
hadynski-next/

# Python cache
__pycache__/
*.pyc
*.pyo

# Backup files
*.backup
*.bak
*.orig
*.old
# WYJĄTEK: .bak w assets/ to oryginalne pliki (przed kompresją) — trzymamy w git jako safety net
!assets/*.bak

# Test files (tymczasowe skrypty audytu)
test_*.html
audit_*.html
install_*.py
mobile_audit.py

# Dev scripts (propagacja zmian między plikami, nie część strony)
_*.py

# Backupy i tymczasowe foldery (nie wchodzą do repo / na produkcję)
_backup_pre_*/
_reel_frames/
_reel_check/
_reel_starts/
_verify_posters/
_scratch/
_leads-setup/

# Claude Code / Cowork local config (per-developer)
.claude/

# Tymczasowe artefakty z bash/sed/awk
*.tmp
*.tmpbak

# Lighthouse CI artifacts
.lighthouseci/
lhci_reports/
